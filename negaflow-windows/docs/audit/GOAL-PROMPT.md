
negaflow Windows 판을 macOS 판과 1:1 이 될 때까지 계속 고쳐라. 끝없는 작업이다.

 먼저 읽어라 (이것부터, 예외 없이)
C:\Users\habin\negaflow\negaflow-windows\docs\audit\00-index.md 부터 전부.
그 안에 macOS 실제 코드와 Windows 실제 코드를 파일·함수 단위로 대조한 결과가 있다.
없는 것·가짜 UI·창작·크래시는 파일명과 줄 번호로 적혀 있다. **문서는 낡을 수 있다 — 코드로 확인해라.**
GPU 0줄·단축키 24개·메모리 캐시 전무·God object 닫음은 낡은 숫자다.
GPU 3.1–3.8은 닫혔다(docs/audit/15). 단축키 열거자 결손은 0이다(docs/audit/09).
FIFO 캐시는 있다(docs/audit/10). God object 생산 6개가 다시 500줄을 넘는다(docs/audit/05).
다시 커널을 이식하지 마라. 새로 넘은 생산 파일은 나누거나 사유를 적어라.
04·15 는 GPU 로 *무엇을* 옮겼고 무엇이 남았는지, 13 은 *어떻게* 빠르게 만들지다.
참고로 프리뷰 이미지나 이미지를 보정/현상하는것과 이미지간 전환은 이미 구현했다.
06 8절·16절에는 이 감사 자신이 틀렸던 것들이 적혀 있다 — 같은 실수를 반복하지 마라.
그 문서의 맨 위 경고를 매 작업마다 다시 읽어라.

문서가 낡아서 오래되거나 잘못된 내용이 있을수 있다. 비판적으로 확인해라.

 절대 하지 마라 (하면 죽는다)
1. 하드코딩 — 값을 박아 증상만 가리는 것
2. 가짜 구현 — 눌러도 아무 일 없는 UI, 호출 안 되는 함수, 값 안 바꾸는 백엔드
3. 창작 — macOS Swift 에 없는 것을 지어내는 것. 상수·임계·순서·아이콘·문구 전부
4. 병신 백엔드 — 대충 만들고 "된다" 고 보고하는 것
5. 병신 프론트엔드 — 모양·크기·위치·정렬·색상·내용이 macOS 와 다르거나 글자가 잘리는 것

추측 금지, 검증해서 해결해라
"아마 이것 때문일 것이다" 로 고치지 마라. 재현하고, 스택을 잡고, 계측해서 원인을 확정한 뒤 고쳐라.
냄새난다고 덮지 말고 냄새의 원인을 찾아 없애라.
예외를 삼키지 말고 왜 났는지 찾아라. 우회 경로를 만들지 말고 깨진 것 자체를 고쳐라.
"느리다" 로 최적화 시작하지 말고 재고 나서 가장 큰 것부터 해라.
원인을 못 잡았으면 "못 잡았다" 고 적어라. 추측으로 고친 것은 다음 사람의 함정이다.

 백엔드 이식 규칙
macOS Swift 파일을 먼저 열고 코드를 1:1 로 그대로 옮겨라. 설명만 보고 다시 쓰지 마라.
파일 하나 고치기 전에: ① 대응 Swift 파일 찾기(못 찾으면 손대지 마라) ② 처음부터 끝까지 읽기
③ Windows 파일과 나란히 놓고 줄 단위로 대기 ④ 다른 곳을 Swift 쪽에 맞추기(반대 방향 금지)
⑤ 주석에 옮겨 온 Swift 심볼 이름 적기.
이름이 같다고 "있음" 으로 적지 마라. 함수 안을 읽고 판정해라.

 프론트엔드 검증 규칙 — 눈으로 봐라
windows-mcp / windows-gui MCP 는 절대 쓰지 마라. 화면은 computer-use 만.
computer-use 도 꼭 필요할 때만 써라. 토큰을 아껴라.
코드·단위시험·스크린샷 50장·이미 있는 로그로 판정되면 띄우지 마라.
화면에 보이는지·눌러서 값이 바뀌는지·잘림/정렬을 이 작업에서 새로 봐야 할 때만 켠다.
켤 때도 전체를 반복 찍지 말고 해당 구역만 크롭(zoom)해라.
세 근거를 전부 쓴 뒤에만 판정해라.
① (필요할 때만) computer-use 로 Windows 앱을 띄우고 구역별로 크롭(zoom)해서 확대해 봐라.
   전체 스크린샷만 보고 판정하지 마라 — 줄어든 그림에서는 2px 도 잘린 글자도 안 보인다.
   앱 실행: scripts\run-app.ps1 -Architecture x64 -Configuration Release
   request_access 는 negaflow.shell.exe (창을 소유한 프로세스 이름)
② Parsec 프로그램으로 macOS negaflow 를 직접 열고 같은 구역을 크롭해서 나란히 대라.
   스크린샷 폴더에 없는 상태(호버·드래그 중·메뉴 열림 위치·비활성·오류)는 Parsec 으로만 확인된다.
   폴더에 있는 설정 10장·메뉴막대 10장은 Parsec 대신이 아니라 **같이** 본다.
   폴더에 없는 것: 스캐너 시뮬레이터 진행, 검토 캡슐·브러시 바·복제 바, 라이브러리 필터/정렬/검색/가져오기 대화상자, 인화 레이아웃 적용 후.
③ C:\Users\habin\맥negaflow 스크린샷\ 50장을 **빠짐없이** 확인해라.
   라이브러리뷰 4 · 현상뷰 22 · 인화뷰 4 · 메뉴막대 10 · 설정 10.
   옛 negaflow_mac_screenshot\ 84장(영문 파일명)은 폐기했다. 쓰지 마라.
   폴더·파일 전체 목록과 구역 대응은 docs/audit/11 1.3절.

일곱 가지를 전부 맞춰라: 모양 · 크기 · 위치 · 정렬 · 색상 · 내용 · 텍스트 안 잘림.
7개 중 하나라도 다르면 그 구역은 "다름" 이다. "대체로 맞음" 은 없다.
XAML 에 요소가 있다고 "있음" 이 아니다 — 화면에 보여야 있는 것이다.
눌러서 값이 안 바뀌면 가짜다. 고칠 때까지 비활성으로 둬라. 되는 것처럼 보이게 두지 마라.

■ 저장소와 라이선스
본체: C:\Users\habin\negaflow\ (Apache License 2.0)
  negaflow-mac\ macOS 원본 Swift — 정답. 읽기만. 절대 고치지 마라
  negaflow-windows\ 여기를 고친다
스캐너 플러그인: C:\Users\habin\negaflow-scanner-sane\ (GPL)
  negaflow-mac\ 와 negaflow-windows\ 로 나뉜다. macOS 쪽은 절대 고치지 마라
SANE 이 GPL 이라 Apache 2.0 본체에 링크하면 배포가 불가능해진다. 그래서 별도 저장소·
별도 프로세스·프로토콜 통신으로 뗀 것이다. 그 경계를 무너뜨리지 마라.
다른 앱 코드 파쿠리 금지(Lightroom·Capture One·Negative Lab Pro·darktable·RawTherapee·
GIMP·VueScan·SilverFast — 소스도 디컴파일도 붙여넣기도 금지).
GPL/LGPL/AGPL 코드를 본체에 넣지 마라. 특허·저작권 확인해라.
출처 없는 상수표·LUT 넣지 마라. SF Symbols 그림 그대로 쓰지 마라(직접 그려라).
새 의존성은 라이선스 확인하고 THIRD_PARTY_NOTICES.md 에 적어라.

■ 검증 = 재현
앱이 죽는다/이상하다는 보고를 받으면 **먼저 그 조작을 그대로 재현해라.**
코드를 읽고 세운 가설로 시험부터 만들지 마라 — 2026-08-20 에 그렇게 만든 스트레스 시험이
10/10 통과했고 원인은 다른 곳이었다(06 17절). 재현이 안 되면 로그·크래시 기록기를 먼저 붙여라
(crash_log.cpp + symbolize-rva.ps1, 01 9.3). 원인은 재현된 증거로만 확정한다.
고친 뒤에도 같은 조작으로 다시 재현해서 안 죽는 것을 확인한 뒤에만 "고쳤다" 고 적어라.

■ 화면을 잴 때
- UI 자동화(UIAutomationClient)로 AutomationId → BoundingRectangle·ToggleState 를 재라.
  이 기계는 150% 배율이라 UIA 값은 물리 화소다. **1.5 로 나눠서** macOS 스크린샷과 견줘라.
- 창을 찍을 때는 PrintWindow + PW_RENDERFULLCONTENT(2) + SetProcessDPIAware().
- 맥 스크린샷 PNG 의 화소값을 색 사양으로 쓰지 마라. iCCP 에 모니터 프로파일이 박혀 있어
  그 숫자는 그 모니터의 장치값이다. **색은 Swift 원본에서, 자리·크기만 스크린샷에서.** (11 6.3)
- 새로 만드는 .ps1 은 **UTF-8 BOM** 으로 저장해라. PowerShell 5.1 은 BOM 이 없으면 cp949 로
  읽어 한글이 다 깨지고 파싱까지 무너진다. (11 6.4)

■ 웹 검색을 적극적으로 써라
모르는 채로 지어내는 것이 창작이다. 찾아서 확인하고 출처를 남겨라.
특히 GPU(D3D11 컴퓨트 셰이더 디스패치, CheckFeatureSupport 기능 수준, UAV/SRV 바인딩,
fxc 빌드 통합, WARP 폴백, Intel 내장/AMD 내장/NVIDIA 제약, 스레드그룹 크기),
최적화(분리형 형태학 van Herk/Gil-Werman, Lanczos, 가이드 필터, 메모리 압력 API, SIMD),
UI/UX(WinUI 3 컨트롤 속성 — Slider.StepFrequency 기본값이 1 인 것을 몰라서 모든 슬라이더가
정수로 스냅됐다, 포커스 타이밍, PathIcon/SVG, 테마 리소스).

■ 작업 순서 (docs/audit/07 G절 — 2026-08-20 갱신)
닫힘: 크래시 A1–A3, 프리뷰 FIFO+2슬롯, 필름 베이스 C1.1–C1.9, 인화 현상본 프리뷰,
메뉴막대 10/11, 초기화, 슬라이더 undo, GPU 3.1–3.8, 비교/줌 HUD 코드, IR 짝짓기,
단축키 열거자, 문자열 9건,
그리고 2026-08-20: 다국어 즉시 전환 + macOS 표 전수 대조(2,670건 중 다른 것 0),
폴더별 현상 프로세스·타깃·적용 단추, 빠른 동작 알약 4개(모양·클릭·글자),
자동 레벨 반복 클릭 강제 종료(develop_export.cpp 소멸 순서),
PowerShell 스크립트 한글 깨짐(UTF-8 BOM). 다시 하지 마라.
지금:
1. 현상 뷰 좌측 프로세스/타깃 UI + 백엔드 — 사용자가 두 번 지적했다 (02 3절 첫 줄)
2. 3뷰(라이브러리·현상·인화) 좌측 세로 레일·좌측탭·우측탭·상단탭 1:1 (02 2.3)
   스크린샷 기준 세로 레일 탭: 라이브러리 3 · 현상 6 · 인화 4. Windows 는 3개만 낸다
3. 앱 실측 — 슬라이더 벽시계, A4 Develop 클릭, IR 쌍 가져오기, 폴더 적용 단추
4. 05 생산 God object 10개 — 나누거나 사유 (DevelopWorkspaceView.xaml.cs 952 가 가장 급하다)
5. 설정 디스크·메모리캐시·지원번들 섹션
6. 내보내기 배치/저널 · 인화 사이드바/템플릿
7. 아이콘 SVG · 필터 캡슐 배치 · 빠른 동작 알약 아이콘 3개(08 5절)
GPU 남은 확인만: 내장 GPU, GpuImagePool 내장 메모리, 64³ 큐브는 별건(15 4절).
죽은 커널 10개를 만들지 마라. 해상도를 깎아 속도를 사지 마라(07 H.9).
사용자가 새로 보고한 것이 있으면 그것을 먼저 확인해라.

■ 문서를 계속 최신화해라 (컨텍스트 압축 대응)
너의 컨텍스트는 압축된다. 기억에 의존하지 말고 md 문서에 남겨라.
- 무엇을 고쳤으면 docs/audit 의 해당 문서를 그 자리에서 갱신해라. 상태를 바꾸고 측정값을 적어라
- 새로 알아낸 것은 새 문서를 만들거나 기존 문서에 절을 추가해라
- docs/audit/07-user-reported.md 에 사용자가 새로 보고한 것을 계속 추가해라
- 매 세션 끝에 docs/progress/ 에 핸드오프를 갱신해라: 무엇을 했고, 무엇을 쟀고,
  무엇을 확인 못 했고, 다음 한 걸음이 무엇인지
- 문서를 갱신하지 않은 커밋은 무엇이 좋아졌는지 모르는 커밋이다
- 주의: .gitignore 112행에 /negaflow-windows/docs/ 가 있어서 새 문서는 git add -f 해야 한다

■ 빌드·시험
scripts\build.ps1 -Preset x64-release 네이티브
scripts\build-managed.ps1 -Preset x64-release 관리
scripts\ci-gate.ps1 -Preset x64-release 로컬 게이트 전체
현재 기준(2026-08-20 x64-release 실측): native **102/102**, catalog **737**,
shell **1384** assertions, 경고 0. 이 숫자가 줄면 안 된다.
native.gpu_film_scan 간헐 SEGFAULT 는 원인을 확정하고 닫았다 — GPU 가 아니라
core/row_block_pool.cpp 의 완료 통지였다(호출부 스택의 PendingCounter 를 워커가 늦게 만짐).
27회 중 3회 실패 -> 292회 연속 통과. 남아 있던 창(뮤텍스를 막 풀고 없애는 순간)도
계수기를 원자값으로 바꿔 닫았고, 성능은 A/B 로 재서 408.24 -> 409.01 ms(차이 0.2%)다.
증거는 01 9.4 · 13 23절.
카탈로그 시험이 가끔 빨간색이던 것은 검사기가 디렉터리 승격을 막던 것이고 고쳤다(문서 17).
GPU 경로마다 CPU 폴백을 두고 두 경로가 같은 값을 내는지 시험으로 고정해라. WARP 로도 돌려라.

■ 보고 규칙
- 시험이 통과했다고 "된다" 고 보고하지 마라. 앱에서 눈으로 확인한 것만 "된다" 이다
- 측정값 없이 "빨라졌다" 고 하지 마라. 이전/이후 ms 를 적어라
- 확인 못 한 것은 "확인 못 했다" 고 적어라
- 커밋 메시지에 무엇을 왜 고쳤는지와 측정값을 적어라
- 커밋에 AI 표시(Co-Authored-By, Generated with 류)를 넣지 마라
- 한국어로 답해라

■ 완료 선언 금지
이것은 끝없는 작업이다. "완료" 라고 확정짓지 마라. "이번 단계 끝" 도 쓰지 마라.
한 항목을 끝내면 문서를 갱신하고 곧바로 다음 항목으로 넘어가라.
macOS 749개 Swift 파일과 Windows 가 전부 대조될 때까지, 그리고 사용자가 앱을 열어
새 문제를 보고하는 한 계속된다. 멈추지 말고 계속 이어나가라.

