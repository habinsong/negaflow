# macOS 픽셀 골든 대조 (2026-08-16)

맥 호스트에서 뽑은 골든(`docs/verification/macos-golden/`)과 Windows 를 **같은 원본**으로
대조했습니다. 원본 SHA-256 이 골든 매니페스트와 일치합니다.

| 원본 | SHA-256 앞 16 | 크기 |
| --- | --- | --- |
| `GT-X900_frame_4.tiff` | `9e3d0daf2537273a` | 2272×3471 |
| `GT-X900_frame_4.tiff.ir.tiff` | `67747cd11919c5aa` | 2272×3471 |

## 일치한 것

### 적외선 정렬·검출 (작업 2) — 전부 일치

| 항목 | macOS | Windows |
| --- | --- | --- |
| coverage | 0.10060967% | 0.1006% |
| clusters | 14 | 14 |
| candidates / confirmed | 120 / 59 | 120 / 59 |
| medianGain | 4 | 4.0000 |
| offset | (1, −1) | (1, −1) |
| peak / runnerUp | 0.012493 / 0.011390 | 0.012 / 0.011 |
| status | aligned | Aligned |

**앞서 이 저장소가 적어 둔 "정렬 상관값 0.017 대조 필요" 는 닫혔습니다.** 0.017 은 다른
스캔(`color_nega.tiff`)의 값이었고, 같은 파일로 재면 어긋남이 없습니다. peak 이 0 에 가까운
것도 정상입니다 — 이 값은 NCC 가 아니라 결함 신호의 밀도 가중평균이고, 판정 임계는
`best.score >= mean × 1.10` 입니다(맥 쪽 `InfraredDefectRemoval+DefectAlignment.swift:121`).
흔히 오해하는 `bestScore > 0.2` 는 누설 상관 전용 경로라 여기에 적용되지 않습니다.

### 인화 기하 (작업 4) — 소수점까지 일치

| 항목 | macOS | Windows |
| --- | --- | --- |
| 이미지(천공 없음) | 2141.5969710787017 | 2141.597 → 2142×3272 |
| 이미지(35mm) | 1510.6656387052742 × 2307.8875140607424 | 1510.666 × 2307.888 → 1511×2308 |
| 필름 사각형 | 2243.779527559055 × 2436.103487064117 @(118.110, 535.948) | 동일 |
| 첫 천공 | 126.93381327334085 × 178.86128233970754 @(230.940, 598.774) | 동일 |
| 천공 반경 | 32.69507311586052 | 32.6951 |
| 규격 2:3 이미지 | 2181 / 1539 | 2181 / 1539 |
| 천공 개수 | 0 / 16 | 0 / 16 |

앞서 폭이 다르게 보였던 것은 제가 다른 원본을 썼기 때문입니다.

## 어긋난 것 — 현상 픽셀

여덟 조건 **전부** 다릅니다. `a-default-main-srgb` 기준(채널별 median / mean):

| 채널 | Windows | macOS | 차이 |
| --- | ---: | ---: | ---: |
| R | 44132 / 40257.4 | 44908 / 40985.7 | −776 / −728 |
| G | 45786 / 41720.2 | 47896 / 43601.6 | −2110 / −1881 |
| B | 47172 / 43200.1 | 49865 / 45653.3 | −2693 / −2453 |

**성질:**

- Windows 가 **일관되게 어둡습니다**. 여덟 조건 모두 같은 방향입니다.
- 차이가 **R < G < B** 로 커집니다. 전역 노출 차이가 아니라 채널에 매인 차이입니다.
- 출력 색공간 세 가지(sRGB · Display P3 · Adobe RGB)가 모두 같은 양상이므로, 원인은
  **색공간 변환보다 앞** 단계입니다.
- 현상 타깃 다섯 가지(MAIN · HS · SP · F135 · HR)도 모두 같은 양상입니다. 타깃 그레이드
  이후가 아니라 그 **앞**입니다.

## 아닌 것으로 확인된 원인 — 자동 base

맥이 잰 auto base 는 다음이고, **전부 float16 정밀도**입니다(Core Image 반정밀도 버퍼).

| 채널 | macOS | Windows | 차이 |
| --- | --- | --- | --- |
| R | 0.338134765625 | 0.338650912046 | +0.000516 |
| G | 0.293212890625 | 0.295434862375 | +0.002222 |
| B | 0.233398437500 | 0.236179560423 | +0.002782 |

초과분이 R < G < B 로 커져 픽셀 차이와 방향이 같아, base 가 원인으로 **보였습니다**.
그래서 맥이 잰 base 를 수동으로 그대로 넣고 다시 냈습니다.

| 채널 | 자동 base | macOS base 강제 | macOS 결과 |
| --- | ---: | ---: | ---: |
| R median | 44132 | 44117 | 44908 |
| G median | 45786 | 45737 | 47896 |
| B median | 47172 | 47108 | 49865 |

**거의 움직이지 않았고, 오히려 조금 멀어졌습니다. base 는 원인이 아닙니다.**

값의 방향이 맞는다고 원인으로 단정하지 않은 것이 이 항목에서 남길 교훈입니다 — 같은 실수를
GrainMend 조사에서 이미 세 번 했습니다. base 를 float16 으로 양자화하는 "수정" 을 먼저 했다면
증상은 그대로인 채 코드만 바뀌었을 것입니다.

## 찾은 원인 — 장면 통계 표본의 축소 방식

밀도 정규화 분모 `dmaxNorm` 은 채널별이고 base 다음·타깃 그레이드 앞에 있습니다. 그 값은
축소본에서 잰 최농부(p0.002)로 정해집니다. 게이팅 상수와 수식은 양쪽이 같았고, **표본을
만드는 축소 방식이 달랐습니다.**

| | macOS | Windows(이전) |
| --- | --- | --- |
| 축소 크기 | 320×488 | 317×484 |
| 축소 방식 | Core Image 렌더 = **면적 평균** | **bilinear 점 표본** |

1/7 로 줄일 때 bilinear 는 칸 안의 50여 화소 중 이웃 넷만 읽습니다. 입자·먼지 같은 극단값이
그대로 남아 p0.002 를 끌어내리고, 그것이 `dmaxNorm` 을 키워 정규화 밀도를 낮추고 결과를
어둡게 만듭니다. 채널마다 잡음이 다르므로 차이도 채널마다 달라집니다.

표본을 그 칸 전체의 면적 평균으로 바꾸고 다시 쟀습니다.

| 채널 | 이전 | 이후 | macOS | 이전 차이 | 이후 차이 |
| --- | ---: | ---: | ---: | ---: | ---: |
| R median | 44132 | 45887 | 44908 | −776 | +979 |
| G median | 45786 | 48115 | 47896 | −2110 | **+219** |
| B median | 47172 | 49624 | 49865 | −2693 | **−241** |
| G max | 59613 | 60393 | 60416 | −803 | **−23** |
| B max | 60392 | 61045 | 61186 | −794 | **−141** |

G·B 가 **열 배 가까이** 줄었습니다. 이 변경은 macOS 를 맞추려는 눈속임이 아니라 그 자체로
옳습니다 — 축소본에서 장면 통계를 잴 때 점 표본은 입자가 퍼센타일을 지배하게 둡니다.

## 표본 격자도 달랐습니다

macOS 는 폭만 320 으로 묶고 높이는 비율대로 두며 **상한이 없습니다**. Windows 에는
`maximum_sample_count = 320×480` 이 있어, 2:3 보다 조금 긴 보통 프레임에서 먼저 걸렸습니다 —
2272×3471 은 320×488 을 원하는데 317×484 로 줄어 다른 퍼센타일을 쟀습니다. 그 상한의 주석이
스스로 "320×480 격자를 지킨다" 고 적어 둔 의도가 바로 그 프레임들에서 깨져 있었습니다.

1:4 까지 허용하도록 올려(원래 목적인 극단 파노라마만 걸림) 다시 쟀습니다.

| 항목 | 면적 평균만 | 격자까지 | macOS |
| --- | ---: | ---: | ---: |
| G max | 60393 | **60416** | 60416 |
| B median | 49624 | 49653 | 49865 |

**G max 가 macOS 와 정확히 같아졌습니다.**

## 아닌 것으로 확인된 원인 2 — 어두운 비필름 게이트

R 잔차가 게이팅의 `R/B` 비(`neutralDarkRatioCut`)에서 오는지 보려고 그 게이트를 끄고
다시 냈습니다. **결과 파일의 SHA-256 이 완전히 같았습니다.** 이 프레임의 base R/B 는
1.434 로 게이트가 켜지는 1.5 에 못 미쳐, 애초에 동작한 적이 없습니다.

두 번째로 방향만 보고 지목했다가 측정에서 걸러진 가설입니다.

## 남은 것 — R 채널 +975

지금 잔차는 R +975 / G +295 / B −212 입니다. 시작할 때의 R −776 / G −2110 / B −2693 에서
G·B 는 열 배 가까이 줄었고 R 만 반대쪽으로 남았습니다(45883 대 44908, 2.2%).

base 는 두 번 다 아니었습니다 — 면적 평균을 적용한 뒤 맥이 잰 base 를 다시 강제해도
R 은 45887→45871 로 16 만 움직였습니다. 게이트도 아니었습니다.

남은 후보는 축소 표본을 만드는 **필터의 모양** 자체입니다. Core Image 의 축소가 정확한
박스 평균이 아니라면(부분 평균이나 밉맵), 세 채널이 각기 다른 만큼 어긋납니다. 다만 단일
혼합 비율로는 설명되지 않습니다 — R 은 필요한 이동의 44%, G 는 91%, B 는 110% 를 받았습니다.
그러므로 "얼마나 평균하느냐" 하나로 맞출 수 있는 문제가 아닙니다. 다음 측정은 축소본
자체의 채널별 퍼센타일을 맥에서 받아 대조하는 것이며, 그러려면 맥 하네스에 그 값을 찍는
항목이 하나 필요합니다.

## 재현

```
PixelGolden.exe <원본.tiff> <출력폴더> docs/verification/macos-golden/task1-pixels/pixel-stats.json
```

`--macbase` 를 덧붙이면 맥이 잰 base 를 수동으로 넣습니다.


## OpticFilm 8100 — 사진이 나오지 않습니다

골든에는 8100 프레임(5136×3543)도 있습니다. 같은 조건으로 대조했습니다.

| | Windows | macOS |
| --- | ---: | ---: |
| base R / G / B | 0.0320 / 0.0120 / 0.0065 | 0.1913 / 0.0939 / 0.0711 |
| R median | **19** | 25353 |
| G median | **6** | 24632 |
| B median | **2** | 22817 |

**median 19 대 25353 — 결과가 거의 새까맣습니다.** V700 의 2% 차이와 달리 이것은 사진이
나오지 않는 수준이며, 사용자의 두 스캐너 중 하나입니다.

base 가 6~11 배 낮게 잡힌 것이 직접 원인입니다. Dmin 이 실제 필름 베이스보다 어두우면
대부분 화소의 밀도가 음수가 되어 검정으로 눌립니다.

### 원본 성질의 차이와, 아닌 것으로 확인된 둘

| | V700(정상) | 8100(까맣게 나옴) |
| --- | --- | --- |
| 엔디안 | II | **MM** |
| 채널 | 3 | **4 (RGBA, ExtraSamples=1)** |
| 압축 | 없음 | **LZW** |

셋 중 무엇인지 가리려고 **정상 동작하는 V700 을 변형**해 두 개를 만들어 돌렸습니다.

| 변형 | 결과 |
| --- | --- |
| RGBA 4채널 + 불투명 알파(ExtraSamples=1) | 원본과 **바이트 동일**(sha 1215ef549475) |
| 빅엔디안(MM) | 원본과 **바이트 동일** |

**엔디안도 알파도 원인이 아닙니다.** 남은 후보는 LZW 압축 경로, 또는 이 이미지의 내용에서
`AutoConnectedComponent` base 추정기가 실패하는 것입니다. macOS 는 같은 파일에서 0.1913 을
찾아냈으므로 파일이 못 읽을 것은 아닙니다.

### 세 후보 모두 배제 — 남은 것은 추정기입니다

| 실험 | 결과 |
| --- | --- |
| V700 을 빅엔디안(MM)으로 변환 | 원본과 **바이트 동일** |
| V700 에 알파 채널 추가(ExtraSamples=1) | 원본과 **바이트 동일** |
| 8100 을 WIC 로 **무압축 3채널**로 다시 기록 후 현상 | base 0.03203 / median 18 — **그대로 검정** |

알파는 한 번 더 확인했습니다. WIC 는 이 파일을 `alpha=Premultiplied` 로 보고하므로 곱해진
알파가 1 미만이면 RGB 가 이미 어둡게 저장돼 있었을 것입니다. 실제 알파 분포는
**min=median=max=65535, 불투명 100%** 였습니다 — 곱셈이 항등이라 알파는 원인이 아닙니다.
(앞서 만든 V700 알파 변형도 불투명 알파였으므로 그 실험만으로는 이 경로를 건드리지
못했을 텐데, 원본 알파가 전부 불투명이라 결과적으로 유효한 배제였습니다.)

**엔디안·알파·압축이 모두 아니면, 남은 것은 `AutoConnectedComponent` 추정기가 이 이미지의
내용에서 실패하는 것입니다.**

### 의심 가는 자리 — 절대 밝기 문턱값

`auto_negative_base_resolver.cpp` 에는 장면과 무관한 절대값이 박혀 있습니다.

| 줄 | 값 | 뜻 |
| --- | --- | --- |
| 152 | `luma <= 0.92` | 후보 상한 |
| 156 | `luma <= 0.85` | 후보 상한(두 번째 경로) |
| 214 | `grid.lumas[index] < 0.88` | 이보다 어두우면 밝은 이웃 배제에서 제외 |
| 281 | `components.front().p75 >= 0.60` | **색 네거티브 2차 성분 선택 자체를 켜는 관문** |
| 453 | `*top < 0.60` | 같은 성격의 관문 |

8100 의 실제 필름 base 는 macOS 측정으로 **0.1913** 입니다. `0.60` 관문은 가장 밝은 성분의
p75 가 0.60 을 넘을 때만 열리므로, 베이스가 어두운 스캔에서는 두 번째 성분(진짜 필름
베이스일 수 있는 쪽)을 고를 기회 자체가 없습니다. V700(0.3387)이 통과한 것은 이 관문을
비껴간 경로였을 뿐, 원리적으로 안전해서가 아닙니다.

### 재어 보니 그 관문도 아니었습니다

추정기에 임시 진단을 넣어 두 프레임에서 실제 값을 찍었습니다(진단은 제거했습니다).

| | 8100 | V700 |
| --- | ---: | ---: |
| 후보 화소 | 40,809 | 100,088 |
| **성분 개수** | **1** | 6 |
| 가장 밝은 성분 p75 | **0.016822** | 0.289698 |
| 성분 셀 수 | 13,252 | 5,485 |
| `floor` (후보 p99 × 0.10) | 0.012199 | 0.029121 |
| gate281 `front.p75 >= 0.60` | **CLOSED** | **CLOSED** |

**gate281 은 두 프레임 모두 닫혀 있습니다.** V700 은 닫힌 채로 올바른 base 를 냅니다.
그러므로 그 관문은 8100 흑화의 원인이 아닙니다 — 여섯 번째 가설도 측정에서 떨어졌습니다.

진짜 모양은 이렇습니다. **8100 에서는 성분이 하나뿐이고, 그것이 0.0168 짜리 어두운
덩어리입니다.** macOS 가 찾아낸 진짜 필름 베이스(R 0.1913 / G 0.0939 / B 0.0711, 휘도
약 0.119)는 성분이 되지 못했습니다. 후보 휘도의 p99 가 0.122 이므로 베이스 영역은 후보
안에 **있지만 상위 1% 남짓**이고, 그 크기로는 성분으로 살아남지 못합니다.

### 다음은 추측이 아니라 사양을 읽는 것입니다

이 알고리즘은 이 저장소의 자체 구현이라 바깥에서 찾을 답이 없습니다. 정답 사양은
`negaflow-mac/Sources/Chromabase/Film/FilmBaseEstimator.swift` 이며, macOS 가 **같은 파일**에서
0.1913 을 어떻게 찾아내는지가 거기 적혀 있습니다. 성분 구성 조건과 최소 크기 기준을
그 구현과 맞대어 읽는 것이 다음 작업입니다.

### 이 조사에서 떨어진 가설 여섯

| 가설 | 어떻게 떨어졌나 |
| --- | --- |
| 자동 base 값 | macOS 값 강제 → R 이 975 중 16 만 이동 |
| 어두운 비필름 게이트 | 끄니 결과 SHA 동일(base R/B 1.434 < 1.5 라 미작동) |
| 빅엔디안 | V700 을 MM 으로 변환 → 바이트 동일 |
| RGBA 알파 | 알파 분포가 전부 65535, 곱셈이 항등 |
| LZW 압축 | 무압축 재기록해도 base 0.03203, 그대로 검정 |
| gate281 `p75>=0.60` | 두 프레임 모두 CLOSED, V700 은 그 상태로 정상 |

여섯 번 모두 방향은 그럴듯했고 여섯 번 모두 틀렸습니다. 재는 데 든 시간이 고쳤다가
되돌리는 것보다 매번 짧았습니다.


## 8100 흑화 — 고쳤습니다

### 원인

macOS 는 base 추정을 **전략 사슬**로 씁니다(`FilmBaseEstimator.estimate`).

```
① connectedBaseComponent   → 증거가 약하면 nil
② sampleBrightOrangeBase / sampleDistributedOrangeBase
③ borderFallback
```

Windows 에도 셋 다 있습니다. 문제는 ① 이 **거절해야 할 때 거절하지 않는 것**이었습니다.
8100 에서 ① 은 성분 하나(p75 0.0168, 후보 봉우리 0.122 의 14%)를 베이스로 채택했고,
그 값이 Dmin 이 되어 대부분 화소의 밀도가 음수가 되며 사진이 검게 눌렸습니다.

### 관문

고른 성분의 p75 가 후보 밝기 봉우리의 절반에 못 미치면 ① 이 포기합니다. 베이스는 오렌지
마스크 후보 가운데 **가장 밝은** 결맞은 구조이므로, 봉우리보다 한참 어두운 성분은 베이스가
아닙니다. 포기하면 ② 가 받습니다.

**강등이 일어난 경우는 제외합니다.** 웜 백라이트 강등은 더 어두운 성분을 일부러 고른
판단이지 증거가 약한 것이 아닙니다. 이 예외를 빠뜨렸다가 네이티브 시험
`color backlight component is demoted below the orange film base` 가 잡아냈습니다.

### 결과

| | 8100 | V700 |
| --- | --- | --- |
| 전략 | ① 거절 → **② 분산 마스크** | ① 그대로 |
| base R | 0.0320 → **0.1764** (macOS 0.1913) | 0.338650912046 (불변) |
| base G | 0.0120 → **0.0929** (macOS 0.0939) | 0.295434862375 (불변) |
| base B | 0.0065 → **0.0703** (macOS 0.0711) | 0.236179560423 (불변) |
| R median | 19 → **23782** (macOS 25353) | 45883 (불변) |
| G median | 6 → **24361** (macOS 24632) | 48191 (불변) |
| 결과 SHA | 바뀜 | **`1215ef549475` — 바이트 동일** |

**V700 이 바이트 단위로 그대로입니다.** 처음에 정한 판정 기준이 이것이었습니다 — 8100 만
좋아지고 V700 이 움직였다면 사진 한 장에 맞춘 것이지 고친 것이 아닙니다.

네이티브 69 개, 셸 902 개 단언 통과.


## base 추정기의 표본 격자도 같은 결함이었습니다

`manual_negative_developer` 의 통계 격자를 면적 평균으로 고쳤지만, base 추정기
(`auto_negative_base_resolver::make_sample_grid`)는 **자기 격자를 따로** 쓰고 거기는 여전히
bilinear 점 표본이었습니다. 같은 이유로 같은 결함입니다 — 크게 줄일 때 이웃 넷만 읽으면
입자가 후보 분포와 성분 경계를 좌우하고, 베이스 찾기는 결맞은 **구조**를 보는 일이라
표본이 시끄러우면 구조가 흩어집니다.

같은 면적 평균을 적용했습니다.

### 8100 — macOS 와 사실상 일치

| 채널 | Windows | macOS | 차이 | 8비트 |
| --- | ---: | ---: | ---: | ---: |
| R median | 25161 | 25353 | −192 | −0.75 |
| G median | 24519 | 24632 | −113 | −0.44 |
| B median | 22783 | 22817 | −34 | −0.13 |
| B mean | 27558.23 | 27563.83 | **−5.6** | −0.02 |
| G max | 62305 | 62305 | **0** | 0 |

base 도 0.18939 / 0.09362 / 0.07095 로 macOS 의 0.19128 / 0.09387 / 0.07111 에 붙었습니다.

**그리고 전략 ① 이 다시 성공합니다**(`source=AutoConnectedComponent`). 표본이 조용해지자
성분이 제대로 형성되어, 앞서 넣은 포기 관문에 걸리지 않고 스스로 옳은 값을 찾습니다.
관문은 그대로 두었습니다 — 표본이 어떤 이유로든 다시 시끄러워질 때를 위한 안전망입니다.

흑화가 두 단계로 닫혔습니다: 관문이 사진을 되살렸고(median 19 → 23782), 격자 수정이
macOS 값까지 데려왔습니다(→ 25161).

### V700 — base 가 네 배 가까이 붙었습니다

| 채널 | 이전 base | 지금 base | macOS | 이전 차이 | 지금 차이 |
| --- | ---: | ---: | ---: | ---: | ---: |
| R | 0.338650912 | 0.337854147 | 0.338134766 | +0.000516 | **−0.000281** |
| G | 0.295434862 | 0.292610824 | 0.293212891 | +0.002222 | **−0.000602** |
| B | 0.236179560 | 0.232825354 | 0.233398438 | +0.002782 | **−0.000573** |

R median 은 45883 → 45860 으로 macOS 44908 대비 +952(이전 +975)입니다. 남은 R 잔차는
여전히 축소 필터의 모양 문제로 보이며, 맥의 축소본 값이 있어야 닫힙니다
(`docs/verification/macos-golden/REQUEST-2.md`).

## 맥 응답(REQUEST-2) 대조 — 필터 하나만 남았습니다

맥이 `4fd7450` 로 프록시 실측값을 보내 왔습니다. 같은 원본, 같은 조건으로 Windows 값을
찍어 나란히 놓았습니다(진단은 제거했습니다).

### 격자·inset·표본 수는 완전히 같습니다

| 항목 | Windows | macOS |
| --- | --- | --- |
| targetW × targetH | 320 × 488 | 320 × 488 |
| inset | (19, 29) | (19, 29) |
| 표본 수 | 121,260 | 121,260 |
| 게이트에서 걸린 화소 | 0 | 0 |

**퍼센타일 규약도 같습니다.** 맥이 우려한 "0.002 가 분수인가 백분율인가" 는 Windows 에서
문제가 아니었습니다 — 구현이 Swift 와 글자 그대로 같은 식입니다.

```
index = min(size - 1, int((size - 1) * fraction))
```

### 남은 차이는 축소 필터 하나

| | Windows | macOS | Windows/macOS |
| --- | ---: | ---: | ---: |
| densest R | 0.06413285 | 0.06365967 | **1.00743** |
| densest G | 0.03219109 | 0.03149414 | **1.02213** |
| densest B | 0.02552585 | 0.02496338 | **1.02253** |
| measuredDmax R | 0.7216 | 0.7252 | — |
| measuredDmax G | 0.9586 | 0.9690 | — |
| measuredDmax B | 0.9600 | 0.9708 | — |

Windows 의 면적 평균은 **세 채널 모두에서 최농부가 덜 어둡습니다**. 박스 평균이 Core Image
어파인 샘플러보다 좁게 섞는다는 뜻입니다 — 넓게 섞을수록 극단이 더 눌리는데, 지금은 덜
눌려 있습니다.

비율이 R 1.0074 / G 1.0221 / B 1.0225 로 **R 만 뚜렷이 작습니다.** 이것이 최종 픽셀에서
R 만 반대 방향으로 남는 이유와 이어집니다.

### 남은 후보와 판정 방법

이제 후보는 하나입니다 — **커널의 모양과 폭**. 맥이 f = 0.005 / 0.01 / 0.05 / 0.1 / 0.5 /
0.9 / 0.99 / 0.998 과 min/max/mean 까지 함께 보냈으므로, 커널 후보를 Windows 에서 돌려
같은 표를 만들고 **여덟 지점이 동시에 맞는지**로 판정할 수 있습니다. 한 지점만 맞추면
그것은 사진 한 장에 맞춘 것입니다.

프록시 f32 파일(2.5MB)을 저장소에 받으면 커널을 몰라도 직접 대조할 수 있어 가장 확실합니다.
