# 핸드오프 — 2026-08-18

## 이번에 한 것

사용자 보고: 앱을 동시에 여러 개 띄우면 고장난다.

Windows App SDK 기본 multi-instance 를 그대로 두고 있어서, 실행마다 새 메인 창과 새 카탈로그 작성자가 생겼다. macOS 는 앱 프로세스 하나다. D-017 이 적어 둔 `AppInstance.FindOrRegisterForKey` + `RedirectActivationToAsync` 를 창·카탈로그보다 앞에 넣었다.

- `src/Shell/Program.cs` — 키 `negaflow.primary` 선출. 두 번째 실행은 활성화만 넘기고 종료
- `src/Shell/App.xaml.cs` — 넘어온 활성화에서 기존 메인 창만 앞으로
- `src/Shell/MainWindow.xaml.cs` — 최소화면이면 복원 후 `Activate()`
- `src/Shell/Negaflow.Shell.csproj` — `DISABLE_XAML_GENERATED_MAIN`

## 잰 것

등록은 그대로 두고 AUMID `Negaflow.Windows_esnvpjf0wq370!App` 만 다시 실행:

- 첫 실행 PID 20384
- 재실행 2회 모두 프로세스 1개, PID 20384 유지
- 그 구간 Application Error / .NET Runtime 이벤트 없음

`run-app.ps1 -SkipBuild` 는 패키지 재등록이라 기존 프로세스를 죽인다. 두 번째 실행 측정이 아니다.

## 확인 못 한 것

- ARM64 런타임에서 같은 선출
- 최소화된 창이 실제로 복원되는지 computer-use 로 보지 못함 (접근성 트리 없음)
## A1 설정 버튼

재현: 작업 옵션 → 설정. 프로세스 종료. Application Error `Microsoft.UI.Xaml.dll` `0xc000027b`, WER `802b000a`.

관리 예외: `SettingsRootView.ctor` → `LocalizeScanTab` → `SetSwitchHeader("developAutoDefect")` → `ResourceLoader.GetString` `COMException 0x80073B17` NamedResource 없음. `TryResource` 가 `InvalidOperationException` 만 잡아 창 생성이 `XamlParseException` 으로 깨짐.

고친 파일: `SettingsRootView.ScanTab.cs`, `AppResources.cs`, `SettingsRootView.xaml`.

검증: 같은 클릭 후 창 제목 `설정`, UIA `settings.category.general` = `일반`. 그 클릭 구간 Application Error 없음.

## 확인 못 한 것

## A2 스캐너 DPI/심도/프레임 규격

재현: 라이브러리 → 스캐너 시뮬레이터 켜기 → 프레임 규격 콤보에서 다른 항목 선택. 프로세스 종료. Application Error `Microsoft.UI.Xaml.dll` `0xc000027b`, WER `8000ffff`.

원인: `ScanSessionController.UpdateOptions` 가 `Changed` 를 올리고 `LibraryScanRenderer.Render` → `FillTagged` 가 열린 ComboBox 에서 `Items.Clear()` 를 호출. WinUI 가 그 순간 네이티브로 죽음.

고친 파일: `LibraryScanRenderer.cs` — 목록 내용이 같을 때는 지우지 않고 선택만 바꾼다.

검증: 같은 경로로 해상도/심도/프레임 규격 변경. 프로세스 PID 34024 유지. 선택값 `7200 dpi` / `16-bit/ch (48-bit)` / `35 mm · 24 × 18`. 그 클릭 구간 Application Error 없음.

## 다음 한 걸음

## 프리뷰 디코드 캐시

`decode.cpp` 에 프로세스 단일 슬롯을 넣었다. 같은 경로·같은 파일 관측이면 `WorkingImage` 를 복사하고 TIFF 를 다시 읽지 않는다. `negaflow_develop_export_abi_tests.exe` 통과.

아직 없는 것: 프레임별 2슬롯, `interactiveProxyDimension` 1024…3600, `waitForDevelopSettle` 0.14초, 정착 프록시 Lanczos. 슬라이더 체감의 나머지(톤·룩 CPU)와 첫 디코드 2,695 ms 는 그대로다.

## 다음 한 걸음

## 2단 렌더 + 0.14초 정착

`DevelopPreviewProxy` 에 macOS 상수와 `interactiveProxyDimension` 식을 옮겼다. `PreviewCoordinator` 는 표시 픽셀 적응 패스 뒤 0.14초 무편집이면 3600 정착을 돌린다. 고정 1600×1200 은 제거.

검증: `test-managed.ps1 -Preset x64-release` catalog 721, shell 1065 assertions. 앱에서 정착 패스 ms 는 안 쟀다.

## 다음 한 걸음

필름 베이스 — 이미지가 검게 터진다. macOS `FilmBaseEstimator` + `Statistics` + `SampleGrid` + `MeasurementDiagnostics` 4파일 대조.

## C14 여러 인스턴스 — 추가 실측 2026-08-18 06:21

사용자 보고: 앱을 동시에 여러 개 띄우면 고장난다.

재현:
- AUMID 동시 2회: 프로세스 1개. Application Error / .NET Runtime 0.
- 이미 떠 있는 앱 위에 exe 직접 실행: 프로세스 1개 유지. 크래시 0.
- 앱을 끈 뒤 exe 직접 실행: 예전에는 DeploymentManager 모듈 초기화와 Application.Start 가 REGDB_E_CLASSNOTREG (0x80040154) 로 죽었다. 지금은 패키지 정체성이 없으면 창을 열지 않고 종료 코드 0.

고친 것:
- WindowsAppSdkDeploymentManagerInitialize=false. 모듈 초기화가 Main 보다 먼저 돌지 않게 함.
- Program.cs 로컬 뮤텍스 Local\Negaflow.Shell.primary 가 COM 보다 앞에서 선출.
- 패키지된 두 번째 실행만 AppInstance 키 negaflow.primary 로 RedirectActivationToAsync.
- 패키지 없이 exe 를 직접 실행하면 Application.Start 를 타지 않음.

확인 못 한 것:
- 최소화된 창이 복원되는지 computer-use 로 보지 못함.
- ARM64 런타임.

## C1 필름 베이스 — 잰 것, 아직 고치지 않음

화면: 현상 뷰, 프레임 OpticFilm8100_frame_7.tiff, 모드 자동, dmin 표시 0.089 / 0.047 / 0.037. 캔버스는 거의 검정.

같은 파일 CLI:
- decode-tiff-wic channel_max [21222,11324,6786] / 65535 = 0.3237 / 0.1728 / 0.1036
- prepare-scanner-tiff transform=embedded_icc_via_windows_icm_srgb16_to_linear_srgb_f32
  channel_min 0.01093 / 0.00121 / 0.000022
  channel_max 0.3237 / 0.1727 / 0.1035

화면 dmin 0.089/0.047/0.037 은 채널 최댓값보다 훨씬 낮다. 반전 식 D=log10(dmin/t) 에서 장면 대부분이 dmin 보다 밝아 검게 앉는다. 원인 확정은 아직 못 했다. 추정기 상수와 반전 소비를 macOS 4파일과 더 대조해야 한다.
