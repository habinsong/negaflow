<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Negaflow.Shell.Views.StatusBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid
        Height="30"
        Padding="10,0"
        Background="{ThemeResource NegaflowBarBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center" Orientation="Horizontal" Spacing="7">
            <Ellipse x:Name="StateIndicator" Width="6" Height="6" />
            <TextBlock x:Name="StateText" VerticalAlignment="Center" FontSize="11" />
            <TextBlock
                x:Name="StateDetail"
                VerticalAlignment="Center"
                FontSize="10"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal" Spacing="10">
            <Button
                Height="22"
                Padding="4,0"
                Background="Transparent"
                BorderThickness="0"
                IsEnabled="False">
                <StackPanel Orientation="Horizontal" Spacing="4">
                    <TextBlock x:Uid="sortInputOrder" FontSize="11" />
                    <FontIcon FontSize="9" Glyph="&#xE74A;" />
                </StackPanel>
            </Button>
            <TextBlock x:Uid="libraryAllShort" VerticalAlignment="Center" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock VerticalAlignment="Center" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="100%" />
            <Button
                Width="24"
                Height="22"
                Padding="0"
                Background="Transparent"
                BorderThickness="0"
                IsEnabled="False">
                <FontIcon FontSize="11" Glyph="&#xE74A;" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
